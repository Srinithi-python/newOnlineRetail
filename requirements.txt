pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
openpyxl>=3.0.7




import pandas as pd
import numpy as np
import matplotlib.pyplot as plt


Included Packagespandas: 
Required for data manipulation, cleaning, aggregation, and loading Excel files (pd.read_excel).  
numpy: Required for vector numerical operations and array data type support. 
 matplotlib: Required for data visualizations and temporal trend plotting. 

%pip install matplotlib
%pip install seaborn

 
 openpyxl: Required as the underlying engine by Pandas to parse and read .xlsx files.  

python -m pip install openpyxl

git hub commands :
-----------------

git init

git remote add origin https://github.com/Srinithi-python/newOnlineRetail.git

git add .
git branch
 git commit -m "Initial commit"

 git config user.name "Srinithi-python"

 git config user.email "srinithikarthikeyan2020@gmail.com"

git commit -m "Initial commit"

 git push -u origin master